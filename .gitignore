# Go 构建产物。`go build ./...` 会把 main 包的可执行文件写到当前目录，
# 一个 server.exe 有 25MB 级别，绝不能进仓库。
*.exe
*.test
*.out

# 交叉编译时（GOOS=linux go build ./...）产出的可执行文件没有扩展名，*.exe 兜不住。
# 按 main 包名逐一挡掉：实测漏过一个 34MB 的 server 文件。
/m365-copilot2api/server
/m365-copilot2api/apktool
/m365-copilot2api/proxy-quality-guard
/m365-copilot2api/token-reissue-probe

# Go 覆盖率与性能剖析输出
coverage.txt
*.prof

# 网关运行时产物（与源码同目录，见 cmd/server/gatewayLogPath）
m365-gateway.log

# 交叉编译给手机的 SOCKS 代理（GOOS=android GOARCH=arm64，见 tools/phone-socks）。
# 没有扩展名，*.exe 挡不住；3.7MB，而且它是部署产物 —— 源码在 tools/phone-socks 里。
/phone-socks

# 退役的批量注册驱动。逻辑已经进网关（internal/web/panel_register_runner.go），
# 留着这两个文件只是历史记录：驱动最后是因为按 GBK 解 UTF-8 把注册结果解坏、
# 连续三批判定「返回无法解析」自己停的。
/register-driver.ps1
/register-driver.log

# 代码索引缓存（codebase-memory MCP）。m365-copilot2api/.gitignore 里那条只管子目录。
.codebase-memory/

# 面板数据目录：内含账密清单与令牌，永远不提交
M365-自用/

# 审计用的旁路构建目录：pprof 剖面、覆盖率快照、临时 sidecar 二进制都落在这里。
# 它是排查过程的产物而不是源码，重建只要重跑一次审计，进仓库只会带来几十 MB 噪声。
/_audit-sidecar/
